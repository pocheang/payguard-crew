# ============================================
# PayGuard 环境配置说明
# ============================================

# 📋 环境文件说明
#
# .env.development  - 开发环境配置（本地开发使用）
# .env.production   - 生产环境配置（生产部署使用）
# .env.example      - 通用配置示例（参考模板）
#
# 根据你的部署环境，复制对应的配置文件为 .env

# ============================================
# 🔄 如何切换环境
# ============================================

# 方式一：手动复制配置文件
# 开发环境：
#   cp .env.development .env
#
# 生产环境：
#   cp .env.production .env

# 方式二：使用部署脚本
# 脚本会自动根据选择的模式使用对应的配置

# 方式三：使用 docker-compose 指定环境文件
# docker-compose --env-file .env.development up
# docker-compose --env-file .env.production up

# ============================================
# 📊 环境对比
# ============================================

# 配置项                 开发环境              生产环境
# ------------------------------------------------------
# APP_ENV               development          production
# DEBUG                 true                 false
# 数据库                SQLite               PostgreSQL
# Redis                 可选                 必需
# JWT 过期时间          24小时               30分钟
# 日志级别              DEBUG                INFO/WARNING
# 速率限制              关闭/宽松            严格启用
# CORS                  允许所有本地源       仅允许指定域名
# 错误详情              显示                 隐藏
# SQL 日志              显示                 关闭
# 热重载                启用                 禁用

# ============================================
# 🔧 开发环境特点
# ============================================
# - 使用 SQLite，无需安装数据库
# - 默认禁用 AI 功能，节省 API 调用
# - 详细的调试日志
# - 宽松的 CORS 和限流策略
# - 长时效的 JWT token
# - 显示详细错误信息

# ============================================
# 🏭 生产环境特点
# ============================================
# - 使用 PostgreSQL + Redis
# - 严格的安全配置
# - 简洁的日志输出
# - 严格的 CORS 和限流
# - 短时效的 JWT token
# - 隐藏错误详情
# - 推荐配置 Sentry 监控

# ============================================
# 🚨 生产环境安全检查清单
# ============================================
# 部署到生产环境前，请确认：
#
# [ ] 所有默认密钥和密码已修改
# [ ] JWT_SECRET_KEY 使用强随机密钥
# [ ] 数据库密码足够强
# [ ] Redis 密码足够强
# [ ] CORS_ORIGINS 仅包含实际域名
# [ ] DEBUG=false
# [ ] LOG_LEVEL 设置为 INFO 或 WARNING
# [ ] RATE_LIMIT_ENABLED=true
# [ ] 已配置 SSL 证书（如果对外服务）
# [ ] 已设置监控和告警
# [ ] 已配置自动备份

# ============================================
# 📖 更多信息
# ============================================
# 查看完整配置说明：
# - .env.development  - 开发环境详细配置
# - .env.production   - 生产环境详细配置
# - DEPLOYMENT.md     - 部署完整指南
